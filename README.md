# HRMS

Basic HRMS service module currently under active development in the `dev` branch.

## Project Module

- Go-based backend module
- Includes config, migration, middleware, database, utilities, and asset service layers
- Main implementation work is currently happening in `dev`
